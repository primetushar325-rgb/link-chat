export default function Home(){ return (<div><h1>Admin — LINK</h1><p style={{color:'#9CA3AF'}}>Use the nav above. Login via API token: set <code>Authorization: Bearer &lt;admin JWT&gt;</code> in fetch. For local dev, login at /admin/login.</p><a href="/admin/login" style={{display:'inline-block',marginTop:12,background:'#6C5CE7',color:'#fff',padding:'10px 16px',borderRadius:8,textDecoration:'none'}}>Admin Login</a></div>); }
